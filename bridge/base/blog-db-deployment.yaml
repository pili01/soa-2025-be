#! blog-db-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: blog-db
    namespace: soa-2025-be
    labels:
        com.docker.compose.project: soa-2025-be
        com.docker.compose.service: blog-db
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: soa-2025-be
            com.docker.compose.service: blog-db
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: soa-2025-be
                com.docker.compose.service: blog-db
                com.docker.compose.network.soa-network: "true"
        spec:
            restartPolicy: Always
            containers:
                - name: postgres-blog-db
                  image: postgres:14-alpine
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: POSTGRES_DB
                      value: "blogSOA"
                    - name: POSTGRES_PASSWORD
                      value: "super"
                    - name: POSTGRES_USER
                      value: "postgres"
                  livenessProbe:
                    exec:
                        command:
                            - /bin/sh
                            - -c
                            - pg_isready -U $POSTGRES_USER -d $POSTGRES_DB
                    periodSeconds: 5
                    timeoutSeconds: 5
                    failureThreshold: 5
                  ports:
                    - name: blog-db-5432
                      containerPort: 5432
                  volumeMounts:
                    - name: var-lib-postgresql-data
                      mountPath: /var/lib/postgresql/data
            volumes:
                - name: var-lib-postgresql-data
                  persistentVolumeClaim:
                    claimName: blog-db-blog-db-data
