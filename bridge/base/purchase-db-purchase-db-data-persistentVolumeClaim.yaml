#! purchase-db-purchase-db-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: purchase-db-purchase-db-data
    namespace: soa-2025-be
    labels:
        com.docker.compose.service: purchase-db
        com.docker.compose.volume: purchase-db-data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
