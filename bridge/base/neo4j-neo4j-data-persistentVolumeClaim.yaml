#! neo4j-neo4j-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: neo4j-neo4j-data
    namespace: soa-2025-be
    labels:
        com.docker.compose.service: neo4j
        com.docker.compose.volume: neo4j-data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
