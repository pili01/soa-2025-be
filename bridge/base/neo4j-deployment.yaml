#! neo4j-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: neo4j
    namespace: soa-2025-be
    labels:
        com.docker.compose.project: soa-2025-be
        com.docker.compose.service: neo4j
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: soa-2025-be
            com.docker.compose.service: neo4j
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: soa-2025-be
                com.docker.compose.service: neo4j
                com.docker.compose.network.soa-network: "true"
        spec:
            restartPolicy: Always
            containers:
                - name: neo4j
                  image: neo4j:4.4
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: NEO4J_AUTH
                      value: "neo4j/supersuper"
                    - name: NEO4J_DB
                      value: "bolt://neo4j:7687"
                    - name: NEO4J_PASSWORD
                      value: "supersuper"
                    - name: NEO4J_URI
                      value: "neo4j://neo4j:7687"
                    - name: NEO4J_USERNAME
                      value: "neo4j"
                    - name: PORT
                      value: "8080"
                  livenessProbe:
                    exec:
                        command:
                            - /bin/sh
                            - -c
                            - echo RETURN 1 | cypher-shell -a bolt://neo4j:7687 -u neo4j -p supersuper || exit 1
                    periodSeconds: 5
                    timeoutSeconds: 5
                    failureThreshold: 10
                  ports:
                    - name: neo4j-7474
                      containerPort: 7474
                    - name: neo4j-7687
                      containerPort: 7687
                    - name: neo4j-7473
                      containerPort: 7473
                  volumeMounts:
                    - name: data
                      mountPath: /data
            volumes:
                - name: data
                  persistentVolumeClaim:
                    claimName: neo4j-neo4j-data
