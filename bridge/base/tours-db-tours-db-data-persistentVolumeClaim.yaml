#! tours-db-tours-db-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: tours-db-tours-db-data
    namespace: soa-2025-be
    labels:
        com.docker.compose.service: tours-db
        com.docker.compose.volume: tours-db-data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
